/**
 * src/site Lenis entry — re-exports the proven LenisProvider (Lenis ↔ GSAP
 * ticker sync, ScrollTrigger.update on scroll, auto-disabled on touch +
 * reduced-motion). One smooth-scroll system for the whole v2 app.
 */
export { default } from '../../components/providers/LenisProvider';
export { useLenis } from '../../components/providers/lenis-context';
